# Game API (ShopBop proxy + game management)
VITE_API_BASE_URL=http://localhost:3000/api

# Backend URL for virtual try-on (same server as game API)
VITE_BACKEND_URL=http://localhost:3000

# Optional: Enable mock data fallback for development (when backend is unavailable)
VITE_USE_MOCK_DATA=false
